<!DOCTYPE html>
<html>
<head>
  <title>User Registration</title>
  <style>
    body { font-family: Arial; margin: 30px; }
    .error { color: red; font-size: 14px; }
    form { max-width: 500px; margin: auto; }
    label { display: block; margin-top: 10px; }
    input { margin-top: 5px; }
  </style>
</head>
<body>
  <h2 style="align-items: center;">User Registration Form</h2>
  <form action="/register" method="POST" enctype="multipart/form-data">
    <label>Username</label>
    <input type="text" name="username" value="<%= old.username || '' %>"/>
    <div class="error"><%= errors.username?.msg || '' %></div>

    <label>Email</label>
    <input type="email" name="email" value="<%= old.email || '' %>"/>
    <div class="error"><%= errors.email?.msg || '' %></div>

    <label>Password</label>
    <input type="password" name="password"/>
    <div class="error"><%= errors.password?.msg || '' %></div>

    <label>Confirm Password</label>
    <input type="password" name="confirmPassword"/>
    <div class="error"><%= errors.confirmPassword?.msg || '' %></div>

    <label>Gender</label>
    <input type="radio" name="gender" value="Male" <%= old.gender==='Male'?'checked':'' %>> Male
    <input type="radio" name="gender" value="Female" <%= old.gender==='Female'?'checked':'' %>> Female
    <div class="error"><%= errors.gender?.msg || '' %></div>

    <label>Hobbies</label>
    <input type="checkbox" name="hobbies" value="Reading" <%= old.hobbies==='Reading'?'checked':'' %>> Reading
    <input type="checkbox" name="hobbies" value="Sports" <%= old.hobbies==='Sports'?'checked':'' %>> Sports
    <input type="checkbox" name="hobbies" value="Music" <%= old.hobbies==='Music'?'checked':'' %>> Music
    <div class="error"><%= errors.hobbies?.msg || '' %></div>

    <label>Profile Picture (single upload)</label>
    <input type="file" name="profilePic"/>
    
    <label>Other Pictures (multiple upload)</label>
    <input type="file" name="otherPics" multiple/>

    <br><br>
    <button type="submit">Register</button>
  </form>
</body>
</html>
